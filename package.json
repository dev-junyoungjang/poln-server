{
  "name": "tcg-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "serverless offline",
    "dev:db": "docker compose up -d && npm run db:create-table && serverless offline",
    "db:start": "docker compose up -d",
    "db:stop": "docker compose down",
    "db:create-table": "aws dynamodb create-table --table-name full-league --attribute-definitions AttributeName=league_id,AttributeType=S --key-schema AttributeName=league_id,KeyType=HASH --billing-mode PAY_PER_REQUEST --endpoint-url http://localhost:8000 --region localhost 2>/dev/null || true",
    "deploy": "serverless deploy",
    "remove": "serverless remove"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.967.0",
    "@aws-sdk/lib-dynamodb": "^3.967.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^20.0.0",
    "serverless": "^3.39.0",
    "serverless-esbuild": "^1.52.0",
    "serverless-offline": "^13.8.0",
    "typescript": "^5.9.3"
  }
}
